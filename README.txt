file list:
preprocess_and_training.py
model.py
finalized_model.sav
final data.csv
USERS.txt
requirements.txt
project.pdf

in the file preprocessing_and_training.py:
divides the data into- train, validation and test, preprocess the data and the
features and train the model according to the processed data.

in the file model.py:
We have implemented the method "classify" as required

in the file finalized_model.sav:
We saved our trained model (after fitting)

in the file final data.csv:
We saved the necessary data for creating feature vectors from the given input